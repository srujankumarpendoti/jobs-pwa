// import { Link } from "react-router-dom";

import { Link } from "react-router-dom";

const Header = () => {
  return (
    <Link to="/" className=" header ">
      <h1 className="header-text">Jobs App</h1>
    </Link>
  );
};

export default Header;
